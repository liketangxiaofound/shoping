# .gitlab-ci.yml
stages:
  - validate
  - build
  - test
  - deploy

# 阶段1: 代码验证
validate-code:
  stage: validate
  script:
    - echo "🔍 开始代码语法检查..."
    - echo "================================="
    - |
      # 检查后端目录是否存在
      if (Test-Path "back") {
        Write-Host "检查后端代码 (back/)..."
        cd back
        $jsFiles = Get-ChildItem -Recurse -Filter *.js
        Write-Host "找到JavaScript文件: $($jsFiles.Count)"
        
        # 检查语法错误
        Write-Host "检查语法错误..."
        $errorCount = 0
        foreach ($file in $jsFiles) {
          if (node -c $file.FullName 2>$null) {
            Write-Host "  ✅ $($file.Name) - 语法正确"
          } else {
            Write-Host "  ❌ $($file.Name) - 语法错误"
            $errorCount++
          }
        }
        
        if ($errorCount -eq 0) {
          Write-Host "✅ 后端代码语法检查通过"
        } else {
          Write-Host "⚠️  发现 $errorCount 个语法错误"
        }
        cd ..
      } else {
        Write-Host "⚠️  后端目录不存在"
      }
    - echo "================================="
  tags:
    - local
  only:
    - main
  allow_failure: false

# 阶段2: 构建
build-job:
  stage: build
  script:
    - echo "编译代码..."
    - echo "编译完成。"
    - |
     if (Test-Path "back") {
        Write-Host "安装后端依赖..."
        cd back
        npm install
        cd ..
        Write-Host "✅ 后端依赖安装完成"
      }
     # 2. 构建前端（如果有）
    - |
      if (Test-Path "front") {
        Write-Host "构建前端..."
        cd front
        npm install
        npm run build
        cd ..
        Write-Host "✅ 前端构建完成"
      }
  tags:
    - local
  only:
    - main

# 阶段3: 测试
unit-test-job:
  stage: test
  script:
    - echo "运行单元测试..."
    - echo "测试完成。"
  tags:
    - local
  only:
    - main
  allow_failure: true

# 阶段4: 半自动部署（需要SSH配置）
deploy-semi-auto:
  stage: deploy
  before_script:
    - |
      Write-Host "🛠️ 配置 SSH 环境..." -ForegroundColor Cyan
      
      $sshDir = "$env:USERPROFILE\.ssh"
      if (-not (Test-Path $sshDir)) {
        New-Item -ItemType Directory -Path $sshDir | Out-Null
      }
      
      # 设置 .ssh 目录权限（仅当前用户）
      icacls $sshDir /inheritance:r /grant:r "$env:USERNAME:(F)" | Out-Null
      
      # 检查 SSH_PRIVATE_KEY
      if ($env:SSH_PRIVATE_KEY) {
        Write-Host "✅ 找到 SSH_PRIVATE_KEY 变量"
        $keyFile = "$sshDir\id_rsa"
        Set-Content -Path $keyFile -Value $env:SSH_PRIVATE_KEY -NoNewline
        
        # 设置私钥权限（仅当前用户读写）
        icacls $keyFile /inheritance:r /grant:r "$env:USERNAME:(R,W)" | Out-Null
        Write-Host "✅ SSH 私钥已配置"
      } else {
        Write-Host "⚠️ 未设置 SSH_PRIVATE_KEY 变量" -ForegroundColor Yellow
        Write-Host "请在 GitLab CI/CD Variables 中添加 SSH_PRIVATE_KEY（类型为 Variable）"
        exit 1
      }
      
      # 测试服务器连通性
      Write-Host "🔌 测试服务器连通性..."
      if (Test-Connection -ComputerName $env:SERVER_IP -Count 1 -Quiet -ErrorAction SilentlyContinue) {
        Write-Host "✅ 服务器可以连通"
      } else {
        Write-Host "❌ 无法 ping 通服务器: $($env:SERVER_IP)" -ForegroundColor Red
        exit 1
      }
  script:
    - |
      Write-Host "🚀 半自动部署到阿里云" -ForegroundColor Cyan
      Write-Host "================================="
      
      $server = $env:SERVER_IP
      $user = $env:SERVER_USER
      $deployPath = $env:DEPLOY_PATH
      $timestamp = Get-Date -Format "yyyyMMdd_HHmmss"
      $sshKeyPath = "$env:USERPROFILE\.ssh\id_rsa"
      
      # 参数校验
      if ([string]::IsNullOrWhiteSpace($server) -or [string]::IsNullOrWhiteSpace($user) -or [string]::IsNullOrWhiteSpace($deployPath)) {
        Write-Host "❌ 缺少必要的环境变量: SERVER_IP, SERVER_USER, DEPLOY_PATH" -ForegroundColor Red
        exit 1
      }
      
      Write-Host "服务器: ${user}@${server}"
      Write-Host "路径: ${deployPath}"
      Write-Host "时间戳: ${timestamp}"
      Write-Host ""
      
      if (Test-Path $sshKeyPath) {
        Write-Host "🔐 检测到 SSH 密钥，开始自动部署..." -ForegroundColor Green
        Write-Host "💡 手动作业已触发，自动继续部署..." -ForegroundColor Green
        
        # 1. 备份
        Write-Host "`n1. 备份现有代码..." -ForegroundColor Yellow
        ssh -i "$sshKeyPath" -o StrictHostKeyChecking=no -o UserKnownHostsFile=NUL "${user}@${server}" "cd '${deployPath}' && tar -czf backup_${timestamp}.tar.gz backend frontend 2>/dev/null || echo '首次部署，跳过备份'"
        
        # 2. 上传后端
        if (Test-Path "back") {
          Write-Host "`n2. 上传后端代码..." -ForegroundColor Yellow
          scp -i "$sshKeyPath" -r -o StrictHostKeyChecking=no -o UserKnownHostsFile=NUL back/* "${user}@${server}:${deployPath}/backend/"
          Write-Host "✅ 后端上传完成" -ForegroundColor Green
        }
        
        # 3. 上传前端
        if (Test-Path "front") {
          Write-Host "`n3. 上传前端代码..." -ForegroundColor Yellow
          scp -i "$sshKeyPath" -r -o StrictHostKeyChecking=no -o UserKnownHostsFile=NUL front/* "${user}@${server}:${deployPath}/frontend/"
          Write-Host "✅ 前端上传完成" -ForegroundColor Green
        }
        
        # 4. 安装依赖并重启服务
        Write-Host "`n4. 安装依赖并重启服务..." -ForegroundColor Yellow
        ssh -i "$sshKeyPath" -o StrictHostKeyChecking=no -o UserKnownHostsFile=NUL "${user}@${server}" "cd '${deployPath}/backend' && npm install --production && pm2 restart all"
        
        Write-Host "`n✅ 部署完成！" -ForegroundColor Green
        Write-Host "🌍 访问地址: http://${server}"
        
      } else {
        Write-Host "❌ 未检测到 SSH 私钥，仅显示手动命令" -ForegroundColor Red
        Write-Host "`n🔧 手动部署命令：" -ForegroundColor Cyan
        Write-Host "================================="
        Write-Host "# 备份"
        Write-Host "ssh -i ~/.ssh/id_rsa ${user}@${server} 'cd ${deployPath} && tar -czf backup_${timestamp}.tar.gz backend frontend'"
        Write-Host ""
        Write-Host "# 上传后端"
        Write-Host "scp -i ~/.ssh/id_rsa -r back/* ${user}@${server}:${deployPath}/backend/"
        Write-Host ""
        Write-Host "# 上传前端"
        Write-Host "scp -i ~/.ssh/id_rsa -r front/* ${user}@${server}:${deployPath}/frontend/"
        Write-Host ""
        Write-Host "# 安装依赖并重启"
        Write-Host "ssh -i ~/.ssh/id_rsa ${user}@${server} 'cd ${deployPath}/backend && npm install --production && pm2 restart all'"
        Write-Host "================================="
      }
  tags:
    - local
  only:
    - main
  when: manual


# 阶段5: 全自动部署（修复 TEMP 路径和权限问题）
deploy-auto:
  stage: deploy
  before_script:
  - |
    Write-Host "🚀 全自动部署初始化..." -ForegroundColor Cyan
    
    # === 1. 确定安全临时路径 ===
    $tempBase = if ([System.IO.Directory]::Exists("$env:USERPROFILE\AppData\Local\Temp")) {
      "$env:USERPROFILE\AppData\Local\Temp"
    } else {
      $env:TEMP
    }
    $sshKeyPath = Join-Path $tempBase ("gitlab_deploy_key_" + (Get-Date -Format "yyyyMMddHHmmss") + "_" + (Get-Random -Minimum 1000 -Maximum 9999))
    
    Write-Host "ℹ️  使用安全临时路径: $sshKeyPath" -ForegroundColor DarkGray

    # === 2. 检查环境变量 ===
    $requiredVars = @("SERVER_IP", "SERVER_USER", "DEPLOY_PATH", "SSH_PRIVATE_KEY")
    $missing = @($requiredVars | Where-Object { [string]::IsNullOrWhiteSpace((Get-Item "env:$_" -ErrorAction SilentlyContinue)?.Value) })
    if ($missing.Count -gt 0) {
      Write-Host "❌ 缺少变量: $($missing -join ', ')" -ForegroundColor Red
      exit 1
    }

    # === 3. 写入标准化私钥 ===
    $key = $env:SSH_PRIVATE_KEY -replace "`r`n", "`n" -replace "`r", "`n"
    if (-not $key.EndsWith("`n")) { $key += "`n" }
    [System.IO.File]::WriteAllBytes($sshKeyPath, [System.Text.Encoding]::UTF8.GetBytes($key))
    Write-Host "✅ 私钥写入成功 (大小: $((Get-Item $sshKeyPath).Length) 字节)" -ForegroundColor Green

    # === 4. 🔑【关键】严格设置私钥权限 ===
    Write-Host "🔒 锁定私钥文件权限..." -ForegroundColor Yellow

    # 获取当前用户名（格式：DOMAIN\user 或 user）
    $identity = [System.Security.Principal.WindowsIdentity]::GetCurrent()
    $userName = $identity.Name

    # 创建全新 ACL（清空所有现有规则！）
    $acl = New-Object System.Security.AccessControl.FileSecurity
    $acl.SetOwner([System.Security.Principal.NTAccount]$userName)
    $acl.SetAccessRuleProtection($true, $false)  # 禁用继承

    # 仅添加当前用户的“读取”权限
    $rule = New-Object System.Security.AccessControl.FileSystemAccessRule(
      $userName,
      "Read",
      "None",
      "None",
      "Allow"
    )
    $acl.AddAccessRule($rule)

    # 应用 ACL
    Set-Acl -Path $sshKeyPath -AclObject $acl

    Write-Host "✅ 权限已严格锁定（仅 $userName 可读）" -ForegroundColor Green

    # === 5. 测试 SSH 连接 ===
    Write-Host "🔑 测试 SSH 连接..." -ForegroundColor Yellow
    $testCmd = "ssh -i `"$sshKeyPath`" -o ConnectTimeout=10 -o BatchMode=yes -o StrictHostKeyChecking=no `"$($env:SERVER_USER)@$($env:SERVER_IP)`" `"echo 'SSH_OK'`""
    $output = & cmd /c "$testCmd" 2>&1
    if ($LASTEXITCODE -eq 0 -and ($output -join "`n") -match "SSH_OK") {
      Write-Host "✅ SSH 认证成功" -ForegroundColor Green
      [System.Environment]::SetEnvironmentVariable("GITLAB_SSH_KEY_PATH", $sshKeyPath, "Process")
    } else {
      Write-Host "❌ SSH 失败: $($output -join "`n")" -ForegroundColor Red
      exit 1
    }
  
  script:
    - |
      Write-Host "🎯 开始全自动部署..." -ForegroundColor Green
      Write-Host "================================="
      
      $sshKeyPath = $env:GITLAB_SSH_KEY_PATH
      if (-not $sshKeyPath -or -not (Test-Path $sshKeyPath)) {
        Write-Host "❌ SSH 密钥文件不存在: $sshKeyPath" -ForegroundColor Red
        Write-Host "💡 可能 before_script 阶段失败" -ForegroundColor Yellow
        exit 1
      }
      
      $server = $env:SERVER_IP
      $user = $env:SERVER_USER
      $deployPath = $env:DEPLOY_PATH
      $timestamp = Get-Date -Format "yyyyMMdd_HHmmss"
      
      Write-Host "部署信息:" -ForegroundColor Cyan
      Write-Host "  - 服务器: ${user}@${server}"
      Write-Host "  - 部署路径: ${deployPath}"
      Write-Host "  - SSH 密钥: $sshKeyPath"
      Write-Host "  - 时间戳: ${timestamp}"
      Write-Host "  - 提交: $env:CI_COMMIT_SHORT_SHA"
      Write-Host "  - 分支: $env:CI_COMMIT_REF_NAME"
      Write-Host ""
      
      # 1. 备份服务器代码
      Write-Host "1️⃣ 备份服务器现有代码..." -ForegroundColor Yellow
      $backupCmd = "cd '${deployPath}' && tar -czf backup_${timestamp}.tar.gz backend frontend 2>/dev/null || echo '首次部署，无需备份'"
      
      $backupJob = Start-Job -ScriptBlock {
        param($keyPath, $user, $server, $backupCmd)
        ssh -i $keyPath -o BatchMode=yes -o StrictHostKeyChecking=no "${user}@${server}" $backupCmd 2>&1
      } -ArgumentList $sshKeyPath, $user, $server, $backupCmd
      
      $backupResult = Wait-Job $backupJob -Timeout 30
      if ($backupResult) {
        $output = Receive-Job $backupJob
        Write-Host "备份结果: $output" -ForegroundColor Gray
        Write-Host "✅ 备份完成" -ForegroundColor Green
      } else {
        Write-Host "⚠️  备份可能超时，继续执行..." -ForegroundColor Yellow
        Stop-Job $backupJob
      }
      Remove-Job $backupJob
      
      # 2. 上传后端代码
      if (Test-Path "back") {
        Write-Host "`n2️⃣ 上传后端代码..." -ForegroundColor Yellow
        $backFileCount = (Get-ChildItem -Recurse -Path "back" | Measure-Object).Count
        Write-Host "发现 $backFileCount 个文件需要上传" -ForegroundColor Gray
        
        # 创建目录
        $mkdirJob = Start-Job -ScriptBlock {
          param($keyPath, $user, $server, $deployPath)
          ssh -i $keyPath -o BatchMode=yes -o StrictHostKeyChecking=no "${user}@${server}" "mkdir -p ${deployPath}/backend" 2>&1
        } -ArgumentList $sshKeyPath, $user, $server, $deployPath
        
        Wait-Job $mkdirJob -Timeout 10 | Out-Null
        Remove-Job $mkdirJob
        
        # 上传文件
        $uploadJob = Start-Job -ScriptBlock {
          param($keyPath, $user, $server, $deployPath)
          $currentDir = Get-Location
          $backPath = Join-Path $currentDir "back"
          scp -i $keyPath -r -o BatchMode=yes "${backPath}/*" "${user}@${server}:${deployPath}/backend/" 2>&1
        } -ArgumentList $sshKeyPath, $user, $server, $deployPath
        
        $uploadResult = Wait-Job $uploadJob -Timeout 60
        if ($uploadResult) {
          $output = Receive-Job $uploadJob
          if ($LASTEXITCODE -eq 0) {
            Write-Host "✅ 后端代码上传完成" -ForegroundColor Green
          } else {
            Write-Host "❌ 后端代码上传失败: $output" -ForegroundColor Red
          }
        } else {
          Write-Host "⚠️  上传超时，继续执行..." -ForegroundColor Yellow
          Stop-Job $uploadJob
        }
        Remove-Job $uploadJob
      } else {
        Write-Host "⚠️  back目录不存在，跳过后端部署" -ForegroundColor Yellow
      }
      
      # 3. 上传前端代码
      if (Test-Path "front") {
        Write-Host "`n3️⃣ 上传前端代码..." -ForegroundColor Yellow
        $frontFileCount = (Get-ChildItem -Recurse -Path "front" | Measure-Object).Count
        Write-Host "发现 $frontFileCount 个文件需要上传" -ForegroundColor Gray
        
        # 创建目录
        $mkdirJob = Start-Job -ScriptBlock {
          param($keyPath, $user, $server, $deployPath)
          ssh -i $keyPath -o BatchMode=yes -o StrictHostKeyChecking=no "${user}@${server}" "mkdir -p ${deployPath}/frontend" 2>&1
        } -ArgumentList $sshKeyPath, $user, $server, $deployPath
        
        Wait-Job $mkdirJob -Timeout 10 | Out-Null
        Remove-Job $mkdirJob
        
        # 上传文件
        $uploadJob = Start-Job -ScriptBlock {
          param($keyPath, $user, $server, $deployPath)
          $currentDir = Get-Location
          $frontPath = Join-Path $currentDir "front"
          scp -i $keyPath -r -o BatchMode=yes "${frontPath}/*" "${user}@${server}:${deployPath}/frontend/" 2>&1
        } -ArgumentList $sshKeyPath, $user, $server, $deployPath
        
        $uploadResult = Wait-Job $uploadJob -Timeout 60
        if ($uploadResult) {
          $output = Receive-Job $uploadJob
          if ($LASTEXITCODE -eq 0) {
            Write-Host "✅ 前端代码上传完成" -ForegroundColor Green
          } else {
            Write-Host "❌ 前端代码上传失败: $output" -ForegroundColor Red
          }
        } else {
          Write-Host "⚠️  上传超时，继续执行..." -ForegroundColor Yellow
          Stop-Job $uploadJob
        }
        Remove-Job $uploadJob
      } else {
        Write-Host "⚠️  front目录不存在，跳过前端部署" -ForegroundColor Yellow
      }
      
      # 4. 安装依赖并重启服务
      Write-Host "`n4️⃣ 安装依赖并重启服务..." -ForegroundColor Yellow
      $restartCmd = @"
        cd '${deployPath}/backend'
        npm install --production
        pm2 restart all || pm2 start npm --name 'backend' -- run start
      "@
      
      $restartJob = Start-Job -ScriptBlock {
        param($keyPath, $user, $server, $restartCmd)
        ssh -i $keyPath -o BatchMode=yes -o StrictHostKeyChecking=no "${user}@${server}" $restartCmd 2>&1
      } -ArgumentList $sshKeyPath, $user, $server, $restartCmd
      
      $restartResult = Wait-Job $restartJob -Timeout 120
      if ($restartResult) {
        $output = Receive-Job $restartJob
        Write-Host "重启结果: $output" -ForegroundColor Gray
        Write-Host "✅ 服务重启完成" -ForegroundColor Green
      } else {
        Write-Host "⚠️  重启超时，但可能已成功" -ForegroundColor Yellow
        Stop-Job $restartJob
      }
      Remove-Job $restartJob
      
      Write-Host "`n🎉 全自动部署完成！" -ForegroundColor Green
      Write-Host "================================="
      Write-Host "🌍 网站地址: http://${server}:3000"
      Write-Host "📅 部署时间: $(Get-Date)"
      Write-Host "🔧 提交哈希: $env:CI_COMMIT_SHORT_SHA"
      Write-Host "🌿 分支: $env:CI_COMMIT_REF_NAME"
      Write-Host "================================="
  
  after_script:
    - |
      Write-Host "🧹 清理临时密钥..." -ForegroundColor Gray
      
      # 尝试从环境变量获取路径
      $sshKeyPath = $env:GITLAB_SSH_KEY_PATH
      
      # 如果环境变量没有，尝试查找临时文件
      if (-not $sshKeyPath -or -not (Test-Path $sshKeyPath)) {
        # 查找可能的临时文件
        $tempDir = [System.IO.Path]::Combine($env:USERPROFILE, "AppData", "Local", "Temp")
        if (Test-Path $tempDir) {
          $possibleFiles = Get-ChildItem -Path $tempDir -Filter "gitlab_deploy_key_*" -ErrorAction SilentlyContinue
          if ($possibleFiles) {
            $sshKeyPath = $possibleFiles[0].FullName
          }
        }
        
        # 如果还没有，尝试另一个目录
        if (-not $sshKeyPath) {
          $possibleFiles = Get-ChildItem -Path $env:TEMP -Filter "gitlab_deploy_key_*" -ErrorAction SilentlyContinue
          if ($possibleFiles) {
            $sshKeyPath = $possibleFiles[0].FullName
          }
        }
      }
      
      if ($sshKeyPath -and (Test-Path $sshKeyPath)) {
        Write-Host "找到临时密钥文件: $sshKeyPath" -ForegroundColor DarkGray
        
        # 🔧 关键修复5: 使用更安全的方法删除文件
        try {
          # 先取消隐藏属性
          attrib -h $sshKeyPath 2>$null
          
          # 尝试直接删除
          Remove-Item -Path $sshKeyPath -Force -ErrorAction Stop
          Write-Host "✅ 临时密钥已删除" -ForegroundColor Green
        } catch {
          Write-Host "⚠️  无法删除文件: $_" -ForegroundColor Yellow
          Write-Host "💡 将在下次部署时覆盖" -ForegroundColor Gray
          
          # 如果无法删除，至少清空文件内容
          try {
            Set-Content -Path $sshKeyPath -Value "" -NoNewline -ErrorAction Stop
            Write-Host "✅ 已清空文件内容" -ForegroundColor Green
          } catch {
            Write-Host "⚠️  无法清空文件内容" -ForegroundColor Yellow
          }
        }
      } else {
        Write-Host "ℹ️  未找到临时密钥文件，可能已删除" -ForegroundColor Gray
      }
  
  tags:
    - local
  only:
    - main
  when: on_success
  environment:
    name: production
    url: http://$env:SERVER_IP:3000