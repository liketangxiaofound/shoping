# .gitlab-ci.yml
stages:
  - validate
  - build
  - test
  - deploy

# 阶段1: 代码验证
validate-code:
  stage: validate
  script:
    - echo "🔍 开始代码语法检查..."
    - echo "================================="
    - |
      # 检查后端目录是否存在
      if (Test-Path "back") {
        Write-Host "检查后端代码 (back/)..."
        cd back
        $jsFiles = Get-ChildItem -Recurse -Filter *.js
        Write-Host "找到JavaScript文件: $($jsFiles.Count)"
        
        # 检查语法错误
        Write-Host "检查语法错误..."
        $errorCount = 0
        foreach ($file in $jsFiles) {
          if (node -c $file.FullName 2>$null) {
            Write-Host "  ✅ $($file.Name) - 语法正确"
          } else {
            Write-Host "  ❌ $($file.Name) - 语法错误"
            $errorCount++
          }
        }
        
        if ($errorCount -eq 0) {
          Write-Host "✅ 后端代码语法检查通过"
        } else {
          Write-Host "⚠️  发现 $errorCount 个语法错误"
        }
        cd ..
      } else {
        Write-Host "⚠️  后端目录不存在"
      }
    - echo "================================="
  tags:
    - local
  only:
    - main
  allow_failure: false

# 阶段2: 构建
build-job:
  stage: build
  script:
    - echo "编译代码..."
    - echo "编译完成。"
    - |
     if (Test-Path "back") {
        Write-Host "安装后端依赖..."
        cd back
        npm install
        cd ..
        Write-Host "✅ 后端依赖安装完成"
      }
     # 2. 构建前端（如果有）
    - |
      if (Test-Path "front") {
        Write-Host "构建前端..."
        cd front
        npm install
        npm run build
        cd ..
        Write-Host "✅ 前端构建完成"
      }
  tags:
    - local
  only:
    - main

# 阶段3: 测试
unit-test-job:
  stage: test
  script:
    - echo "运行单元测试..."
    - echo "测试完成。"
  tags:
    - local
  only:
    - main
  allow_failure: true

# 阶段4: 半自动部署（需要SSH配置）
deploy-semi-auto:
  stage: deploy
  before_script:
    - |
      # 全部用 PowerShell 写（因为 tags: local 很可能是 Windows）
      Write-Host "🛠️ 配置 SSH 环境..." -ForegroundColor Cyan
      
      # 创建 .ssh 目录
      $sshDir = "$env:USERPROFILE\.ssh"
      if (-not (Test-Path $sshDir)) {
        New-Item -ItemType Directory -Path $sshDir | Out-Null
      }
      & chmod 700 $sshDir
      
      # 检查是否设置了 SSH_PRIVATE_KEY（GitLab CI 变量）
      if ($env:SSH_PRIVATE_KEY) {
        Write-Host "✅ 找到 SSH_PRIVATE_KEY 变量"
        # 将私钥内容写入文件（注意：变量是密钥内容，不是路径！）
        Set-Content -Path "$sshDir\id_rsa" -Value $env:SSH_PRIVATE_KEY -NoNewline
        & chmod 600 "$sshDir\id_rsa"
        Write-Host "✅ SSH 私钥已配置"
      } else {
        Write-Host "⚠️  未设置 SSH_PRIVATE_KEY 变量" -ForegroundColor Yellow
        Write-Host "请在 GitLab CI/CD Variables 中添加 SSH_PRIVATE_KEY（类型为 File 或 Variable）"
      }
      
      # 测试服务器连通性（ping）
      Write-Host "🔌 测试服务器连通性..."
      $pingResult = Test-Connection -ComputerName $env:SERVER_IP -Count 1 -Quiet -ErrorAction SilentlyContinue
      if ($pingResult) {
        Write-Host "✅ 服务器可以连通"
      } else {
        Write-Host "❌ 无法 ping 通服务器: $($env:SERVER_IP)" -ForegroundColor Red
        exit 1
      }
  script:
    - |
      Write-Host "🚀 半自动部署到阿里云" -ForegroundColor Cyan
      Write-Host "================================="
      
      $server = $env:SERVER_IP
      $user = $env:SERVER_USER
      $deployPath = $env:DEPLOY_PATH
      $timestamp = Get-Date -Format "yyyyMMdd_HHmmss"
      
      # 参数校验
      if ([string]::IsNullOrWhiteSpace($server) -or [string]::IsNullOrWhiteSpace($user) -or [string]::IsNullOrWhiteSpace($deployPath)) {
        Write-Host "❌ 缺少必要的环境变量: SERVER_IP, SERVER_USER, DEPLOY_PATH" -ForegroundColor Red
        exit 1
      }
      
      Write-Host "服务器: $user@$server"
      Write-Host "路径: $deployPath"
      Write-Host "时间戳: $timestamp"
      Write-Host ""
      
      $sshKeyPath = "$env:USERPROFILE\.ssh\id_rsa"
      $hasSshKey = Test-Path $sshKeyPath
      
      if ($hasSshKey) {
        Write-Host "🔐 检测到 SSH 密钥，可以尝试自动上传" -ForegroundColor Green
        Write-Host ""
        Write-Host "💡 手动作业已触发，自动继续部署..." -ForegroundColor Green
        
        # 1. 备份
        Write-Host "`n1. 备份现有代码..." -ForegroundColor Yellow
        ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=NUL "${user}@${server}" "cd '${deployPath}' && tar -czf backup_${timestamp}.tar.gz backend frontend 2>/dev/null || echo '首次部署，跳过备份'"
        
        # 2. 上传后端
        if (Test-Path "back") {
          Write-Host "`n2. 上传后端代码..." -ForegroundColor Yellow
          scp -r -o StrictHostKeyChecking=no -o UserKnownHostsFile=NUL back/* "${user}@${server}:${deployPath}/backend/"
          Write-Host "✅ 后端上传完成" -ForegroundColor Green
        }
        
        # 3. 上传前端
        if (Test-Path "front") {
          Write-Host "`n3. 上传前端代码..." -ForegroundColor Yellow
          scp -r -o StrictHostKeyChecking=no -o UserKnownHostsFile=NUL front/* "${user}@${server}:${deployPath}/frontend/"
          Write-Host "✅ 前端上传完成" -ForegroundColor Green
        }
        
        # 4. 安装依赖并重启
        Write-Host "`n4. 安装依赖并重启服务..." -ForegroundColor Yellow
        ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=NUL "${user}@${server}" "cd '${deployPath}/backend' && npm install --production && pm2 restart all"
        
        Write-Host "`n✅ 部署完成！" -ForegroundColor Green
        Write-Host "🌍 访问地址: http://${server}"
        
      } else {
        Write-Host "❌ 未检测到 SSH 私钥，仅显示手动命令" -ForegroundColor Red
        Write-Host "`n🔧 手动部署命令：" -ForegroundColor Cyan
        Write-Host "================================="
        Write-Host "# 备份"
        Write-Host "ssh ${user}@${server} 'cd ${deployPath} && tar -czf backup_${timestamp}.tar.gz backend frontend'"
        Write-Host ""
        Write-Host "# 上传后端"
        Write-Host "scp -r back/* ${user}@${server}:${deployPath}/backend/"
        Write-Host ""
        Write-Host "# 上传前端"
        Write-Host "scp -r front/* ${user}@${server}:${deployPath}/frontend/"
        Write-Host ""
        Write-Host "# 安装依赖并重启"
        Write-Host "ssh ${user}@${server} 'cd ${deployPath}/backend && npm install --production && pm2 restart all'"
        Write-Host "================================="
      }
  tags:
    - local
  only:
    - main
  when: manual